# Database Configuration
# Get your Neon PostgreSQL connection string from: https://neon.tech
# Format: postgresql://[user]:[password]@[host]/[dbname]?sslmode=require
DATABASE_URL="postgresql://user:password@your-neon-host.neon.tech/trainsync?sslmode=require"

# AI Configuration
# Get your Gemini API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY="your-gemini-api-key-here"

# Session Security
# Generate a random secret with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="your-session-secret-here"

# Cron Job Security
# Generate a random secret with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# This secret authenticates requests to /api/cron/* endpoints
CRON_SECRET="your-cron-secret-here"

# Frontend Configuration (Optional)
# Specify your frontend URL for CORS in production
# If not set, CORS will allow all origins
FRONTEND_URL="https://your-app.vercel.app"

# Environment
NODE_ENV="production"

# Port (Vercel will set this automatically)
PORT="5000"
